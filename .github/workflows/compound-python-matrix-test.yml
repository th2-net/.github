name: Run matrix Python test


on:
  workflow_call:
    inputs:
      test-dir:
        required: true
        type: string
        description: 'Dirrectiry with pytests'
      test-ignore-dir:
        required: false
        type: string
        default: ''
        description: 'Python version'
      test-requirments-file:
        required: false
        type: string
        default: ''
        description: 'Path to reuqirments'
      matrix-python-versions:
        required: false
        type: string
        default: "['3.8', '3.9']"
        description: 'list of python versions in JSON format'
      matrix-os:
        required: false
        type: string
        default: "['ubuntu-20.04', 'windows-latest']"
        description: 'list of operation systems JSON format'
      runsOn:
        required: false
        type: string
        default: 'ubuntu-latest'

jobs:
  matrix-test:
    name: Run matrix test
    strategy:
      matrix:
        python-version: ${{ fromJson(inputs.matrix-python-versions) }}
        os: ${{ fromJson(inputs.matrix-os) }}
    uses: th2-net/.github/.github/workflows/python-test.yml@python-workflow # FIXME: change to main
    with:
      python-version: ${{ matrix.python-version }}
      test-dir: ${{ inputs.test-dir }}
      test-ignore-dir: ${{ inputs.test-ignore-dir }}
      requirments-file: ${{ inputs.test-requirments-file }}
      runsOn: ${{ matrix.os }}
